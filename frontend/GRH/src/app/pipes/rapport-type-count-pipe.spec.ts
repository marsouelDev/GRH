import { RapportTypeCountPipe } from './rapport-type-count-pipe';

describe('RapportTypeCountPipe', () => {
  it('create an instance', () => {
    const pipe = new RapportTypeCountPipe();
    expect(pipe).toBeTruthy();
  });
});
