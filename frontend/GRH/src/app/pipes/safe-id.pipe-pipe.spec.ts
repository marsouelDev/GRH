import { SafeIdPipePipe } from './safe-id.pipe-pipe';

describe('SafeIdPipePipe', () => {
  it('create an instance', () => {
    const pipe = new SafeIdPipePipe();
    expect(pipe).toBeTruthy();
  });
});
